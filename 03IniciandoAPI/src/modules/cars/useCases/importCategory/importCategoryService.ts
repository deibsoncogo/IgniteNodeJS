class ImportCategoryService { // grupo único e principal
  execute(file: any) { // função única e principal
    return file; // retorna algo ao chamador
  }
}

export { ImportCategoryService }; // exporta para poder ser chamado
